#include <stdio.h>
#include<pthread,semaphore,unistd,string>

sem_t mutex;
int sc,sc; =0;

if north

	while south > 0
		sem_wait(&mutex)
north++

else 

vv

critical

if north 
n--
else vv





int main

sem_t init(&mutex,0,1);

pthread_t  t1 t2 t3

char *msg = n, s;

pthread_create(&t1,NULL,fp,arg);

pthread_join(&t1,NULL);

sem_destroy(&mutex);


